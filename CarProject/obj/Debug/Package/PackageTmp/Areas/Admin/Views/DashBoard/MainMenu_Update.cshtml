@{
    ViewBag.Title = "ویرایش منو صفحه اصلی";
}
@using CarProject.App_Code
@model CarProject.DBSEF.HomePageMenu


<div class="page">
    <h3>@ViewBag.Title</h3>
    <form method="post">
        <div class="inputsContainer">
            @Html.input_TextBox(m => m.Subject, "عنوان")
            @Html.input_TextBox(m => m.Title, "توضیحات")
            @Html.input_TextBox(m => m.Target, "لینک")
        </div>
        <div class="inputAction">
            <input type="submit" value="بروزرسانی" class="button green" />
        </div>
    </form>
</div>
