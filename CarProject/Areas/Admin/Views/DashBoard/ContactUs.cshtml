@using CarProject.App_Code
@model CarProject.Areas.Admin.Models.Dashboard.ContactUs
@{
    ViewBag.Title = "تماس با ما";
}


<div>
    
    <form method="post">
        <div class="inputsContainer">
            @Html.input_TextArea(m => m.ShortDescribe, "توضیحات مختصر")
            @Html.input_TextArea(m => m.Html, "صفحه صفحه تماس با ما")
        </div>
        <div class="inputAction">
            <input type="submit" class="button green" value="ثبت" />
        </div>
    </form>
</div>

@Html.Action("tinymcefm2", "FileManager", new { id = Html.IdFor(m => m.Html) })