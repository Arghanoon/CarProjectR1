@{
    ViewBag.Title = "تاریخچه خرید";

    var dbs = new CarProject.DBSEF.CarAutomationEntities();
    var user = Session["guestUser"] as CarProject.DBSEF.User;
    var baskets = dbs.Baskets.Where(c => c.UserId == user.UserId);
}
@using CarProject.App_extension



<div class="page">
    <table class="table">
        <thead>
            <tr>
                <th class="fix">شماره</th>
                <th class="fix">تاریخ</th>
                <th class="fix">کد پیگیر بانک</th>
                <th>کد پیگیری</th>
                <th>عملیات</th>
            </tr>
        </thead>
        <tbody>
            @{int i = 1; }
            @foreach (var item in baskets)
            {
                <tr>
                    <td class="center">@i</td>
                    <td class="fix">@item.date.DateTime_Persian()</td>
                    <td>@item.BackCodeFromBank</td>
                    <td>@item.BasketCode</td>

                    <td class="center">
                        <a href="/Users/Dashboard/BasketDetails/@item.BasketId" class="gia-details" ></a>
                    </td>
                </tr>
                
                i += 1;
            }
        </tbody>
    </table>
</div>



@section head
{
    <style>
        .table {
            width:100%;
        }

            .table td,
            .table th {
                padding:1px 3px;
                border:thin solid #eaeaea;
            }

            .table thead {
                background:#eaeaea;
            }

            .table .fix {
                width:0;
                white-space:nowrap;
            }

            .table .center {
                text-align:center;
            }

    </style>
}